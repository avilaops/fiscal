# Copy this file to .env locally (do NOT commit secrets)
# For production (Cloud Run/App Engine), set these as environment variables in the service configuration/runner.

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=False
DJANGO_SETTINGS_MODULE=fiscal.settings
ALLOWED_HOSTS=fiscal.aviladevops.com.br,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://fiscal.aviladevops.com.br

# Database (Cloud SQL MySQL)
DB_NAME=y
DB_USER=sql
DB_PASS=7Aciqgr7@

# Choose ONE connection mode
# 1) GCP socket (recommended in GCP)
INSTANCE_CONNECTION_NAME=nicolasrosaab:us-central1:sql
# If using socket, your Django settings should use HOST=/cloudsql/${INSTANCE_CONNECTION_NAME}

# 2) Public IP (local dev via proxy or authorized IP)
# DB_HOST=35.222.79.14
# DB_PORT=3306

# GCP context
GOOGLE_CLOUD_PROJECT=nicolasrosaab

# Static/Media
STATIC_URL=/static/
MEDIA_URL=/media/
